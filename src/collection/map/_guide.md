# Sequence

- HashMap
    - HashMapOps
    - WaysToCreateHashMap
- LinkedHashMap
    - InsertionOrder
    - AccessOrderTrue
    - LRUCache
- TreeMap
    - Map -> sorted and general Map ops
    - SortedMap -> sorted and basic navigation firstKey, lastKey (headmap, tailmap, submap)
    - NavigableMap/TreeMap just like Map/HashMap -> (headmap, tailmap, submap)
        floorKey(k)    // closest <= k
        ceilingKey(k) // closest >= k
        lowerKey(k)   // strictly <
        higherKey(k)  // strictly >